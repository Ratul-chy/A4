1 .Difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll is  getElementById only calls the id and getElementsByClassName only calles the class
querySelector only takes the first item querySelectorall takes the all matching items .

2 by creating new element and Add content to the element then appendchild . it Insert the new element into DOM .

3. Event Bubbling is an event starts from the target element and then moves upward through its parent elements.

4. Event Delegation is adding one event listener to a parent element instead of multiple children.

5. Difference between preventDefault() and stopPropagation() is preventDefault() Stops browser’s default behavior and stopPropagation() Stops event from bubbling upward
